# Skill Factory - 技能工厂

## 概述
Skill Factory 是一个 Claude Code 元技能，用于生成其他 Claude Code 技能。它接收用户对目标技能的描述，然后自动生成完整的技能包，包括技能定义、工具配置、示例代码、测试用例和文档。

## 核心特性
- 🏭 **自动生成**：根据描述生成完整的技能结构
- 🎯 **模板系统**：支持多种技能类型模板（协调器、专业工具、集成器、分析器）
- 🔧 **工具集成**：自动配置工具权限和依赖
- 📚 **完整文档**：生成使用文档、API参考和开发指南
- 🧪 **测试覆盖**：包含单元测试和集成测试
- 🔄 **可扩展**：支持自定义模板和插件

## 快速开始

### 基本使用
```bash
# 使用技能工厂生成新技能
skill-factory skill_name="my-new-skill" \
  description="我的新技能描述" \
  primary_function="主要功能说明" \
  tools_needed="read,write"
```

### 完整示例
```json
{
  "skill_name": "code-review-assistant",
  "skill_type": "specialist",
  "description": "代码审查助手，自动分析代码质量",
  "primary_function": "自动化代码审查和建议",
  "target_users": "开发者、学生",
  "tools_needed": ["read", "edit", "bash"],
  "use_cases": ["代码质量检查", "学习最佳实践"],
  "output_format": "full-package"
}
```

## 安装（如果 Claude Code 支持技能安装）

### 方法1：从本地安装
```bash
# 克隆或下载技能工厂
git clone https://github.com/cailleachzou/skill-factory.git

# 安装到 Claude Code
claude-code skill install ./skill-factory
```

### 方法2：从技能市场安装（如果可用）
```bash
# 搜索技能工厂
claude-code skill search "skill factory"

# 安装技能
claude-code skill install skill-factory
```

## 使用方法

### 1. 生成基本技能
```bash
skill-factory --input '{
  "skill_name": "simple-helper",
  "description": "简单的辅助工具",
  "primary_function": "提供基础帮助功能"
}'
```

### 2. 生成协调器技能
```bash
skill-factory --input '{
  "skill_name": "workflow-coordinator",
  "skill_type": "coordinator",
  "description": "工作流协调器",
  "primary_function": "协调多个子技能执行",
  "tools_needed": ["read", "write", "context"]
}'
```

### 3. 生成集成器技能
```bash
skill-factory --input '{
  "skill_name": "git-helper",
  "skill_type": "tool-integration",
  "description": "Git版本控制助手",
  "primary_function": "Git仓库管理和分析",
  "tools_needed": ["bash", "read"]
}'
```

### 4. 使用自定义模板
```bash
skill-factory --input '{
  "skill_name": "custom-skill",
  "skill_type": "specialist",
  "description": "自定义技能",
  "custom_template": "./my-templates/specialized-template"
}'
```

## 配置选项

### 输入参数

| 参数 | 类型 | 必填 | 描述 | 示例 |
|------|------|------|------|------|
| `skill_name` | string | ✅ | 技能名称（小写，连字符分隔） | `code-review-helper` |
| `description` | string | ✅ | 技能详细描述 | `代码审查助手...` |
| `primary_function` | string | ✅ | 主要功能说明 | `自动化代码审查` |
| `skill_type` | string | ❌ | 技能类型 | `specialist` |
| `target_users` | string | ❌ | 目标用户 | `开发者、学生` |
| `tools_needed` | array | ❌ | 所需工具 | `["read", "edit"]` |
| `use_cases` | array | ❌ | 使用场景 | `["代码审查", "学习"]` |
| `output_format` | string | ❌ | 输出格式 | `full-package` |

### 技能类型选项
- `coordinator`：协调器技能（管理多个子技能）
- `specialist`：专业工具技能（特定领域功能）
- `tool-integration`：集成器技能（连接外部工具）
- `learning-analytics`：分析器技能（数据分析和学习）

### 输出格式选项
- `full-package`：完整技能包（包含所有文件）
- `minimal`：最小化包（仅核心文件）
- `template-only`：仅模板文件

## 生成的文件结构

### 完整包格式（full-package）
```
generated-skill/
├── skill-definition/
│   └── skill.json           # 技能定义
├── tools/
│   ├── tools.json           # 工具配置
│   └── [implementation].js  # 工具实现
├── examples/
│   ├── basic-example.md     # 基础示例
│   └── advanced-example.md  # 高级示例
├── tests/
│   ├── unit-tests.js        # 单元测试
│   └── integration-tests.js # 集成测试
├── docs/
│   ├── README.md            # 使用文档
│   ├── API-reference.md     # API参考
│   └── development-guide.md # 开发指南
└── implementation.md        # 实现说明
```

### 最小化包格式（minimal）
```
generated-skill/
├── skill-definition/
│   └── skill.json
├── tools/
│   └── tools.json
└── README.md
```

## 技能模板系统

### 内置模板

#### 1. 协调器模板（coordinator）
用于创建管理多个子技能的元技能。
- **特点**：状态管理、工作流引擎、子技能协调
- **适用**：Vibe Writing 协调器、工作流管理器

#### 2. 专业工具模板（specialist）
用于创建特定领域的专业工具。
- **特点**：领域算法、专业功能、优化建议
- **适用**：代码审查助手、文档分析器

#### 3. 集成器模板（tool-integration）
用于集成外部工具和服务。
- **特点**：API集成、数据转换、协议支持
- **适用**：Git集成器、数据库连接器

#### 4. 分析器模板（learning-analytics）
用于数据分析和学习。
- **特点**：数据分析、模式识别、学习跟踪
- **适用**：代码质量分析器、学习进度跟踪器

### 自定义模板
创建自定义模板：
1. 在 `templates/` 目录下创建新模板文件夹
2. 创建 `template-config.json` 配置文件
3. 实现模板文件，使用 `{{variable}}` 语法
4. 在生成时通过 `custom_template` 参数指定

## 工具权限系统

### 可用工具
| 工具 | 权限 | 描述 |
|------|------|------|
| `read` | `files:read` | 读取文件内容 |
| `write` | `files:write` | 写入新文件 |
| `edit` | `files:edit` | 编辑现有文件 |
| `glob` | `files:search` | 搜索文件模式 |
| `webfetch` | `network:fetch` | 获取网页内容 |
| `websearch` | `network:search` | 进行网络搜索 |
| `bash` | `system:execute` | 执行系统命令 |
| `context` | `context:read/write` | 管理对话上下文 |

### 权限配置建议
- **基础技能**：`read`, `write`
- **网络技能**：`webfetch`, `websearch`
- **系统技能**：`bash`, `read`
- **协调技能**：`context`, `read`, `write`

## 高级功能

### 批量生成
```bash
# 从配置文件批量生成
skill-factory --batch skills-config.json

# skills-config.json 格式：
[
  {
    "skill_name": "skill-1",
    "description": "..."
  },
  {
    "skill_name": "skill-2",
    "description": "..."
  }
]
```

### 技能升级
```bash
# 升级现有技能
skill-factory --upgrade existing-skill/ --input '{
  "new_features": ["新增功能1", "新增功能2"]
}'
```

### 技能验证
```bash
# 验证生成的技能
skill-factory --validate generated-skill/

# 验证结果包括：
# - 文件结构完整性
# - 配置正确性
# - 工具权限一致性
```

## 故障排除

### 常见问题

#### 1. 技能生成失败
**错误**：`Template not found`
**解决**：检查 `skill_type` 是否正确，或提供有效的 `custom_template` 路径

#### 2. 权限错误
**错误**：`Invalid tool permission`
**解决**：检查 `tools_needed` 列表，确保使用有效的工具名称

#### 3. 文件冲突
**错误**：`File already exists`
**解决**：使用不同的输出目录，或启用覆盖模式 `--force`

#### 4. 内存不足
**错误**：`Memory allocation failed`
**解决**：减少生成的文件数量，使用 `minimal` 输出格式

### 调试模式
```bash
# 启用详细日志
skill-factory --input '...' --verbose

# 保存中间文件
skill-factory --input '...' --save-temp

# 性能分析
skill-factory --input '...' --profile
```

## 最佳实践

### 1. 技能设计
- **明确功能**：技能功能应该单一明确
- **合理命名**：使用小写和连字符，反映功能
- **最小权限**：只请求必要的工具权限
- **完整文档**：提供清晰的使用文档

### 2. 生成策略
- **测试优先**：先生成最小化版本进行测试
- **迭代开发**：根据测试结果逐步完善
- **版本控制**：为生成的技能设置版本号
- **备份配置**：保存生成参数以便重现

### 3. 性能优化
- **模板缓存**：重复使用已加载的模板
- **并行生成**：独立文件可以并行生成
- **增量更新**：只更新变化的文件

## 开发指南

### 扩展技能工厂

#### 添加新模板
1. 创建模板文件夹 `templates/new-template/`
2. 实现模板文件
3. 更新模板注册表
4. 测试模板功能

#### 添加插件
1. 创建插件文件 `plugins/my-plugin.js`
2. 实现插件接口
3. 配置插件启用
4. 验证插件功能

### 贡献指南
1. Fork 项目仓库
2. 创建功能分支
3. 实现新功能
4. 添加测试用例
5. 提交 Pull Request

## 相关资源

### 文档
- [API 参考](./docs/API-reference.md)
- [开发指南](./docs/development-guide.md)
- [模板系统](./templates/)

### 示例
- [基础使用示例](./examples/basic-example.md)
- [代码审查助手示例](./examples/code-review-helper/)

### 工具
- [工具配置](./tools/tools.json)

## 许可证
MIT License - 详见 [LICENSE](./LICENSE) 文件

## 支持
- **问题报告**：[GitHub Issues](../../issues)
- **讨论区**：[GitHub Discussions](../../discussions)
- **文档更新**：提交文档改进的 Pull Request

---

**提示**：Skill Factory 是为 Claude Code 技能系统设计的原型工具。实际使用时可能需要根据 Claude Code 的具体要求进行调整。
